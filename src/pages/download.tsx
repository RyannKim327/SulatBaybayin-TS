import _title from "../utils/title"

const DL = (props: any) => {
  _title("Download Sulat Baybayin App")
  return (
    <div className={`${props.className}`}>
      <blockquote>
        Hello World
      </blockquote>
    </div>
  )
}

export default DL
