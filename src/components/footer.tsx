const Footer = (props: any) => {
  return (
    <div className={`${props.className} flex justify-center items-center`}>
      <h3>Develped by MPOP Reverse II</h3>
    </div>
  );
};

export default Footer;
